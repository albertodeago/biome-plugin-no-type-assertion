/**
 * Fixture to check that import aliases using `as` are allowed.
 * This is NOT a type assertion - it's just renaming an import.
 */
import { Inter as FontSans } from "next/font/google";

console.log(FontSans);
